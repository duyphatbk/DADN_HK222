import { View, Text } from 'react-native'
import React from 'react'
import {
    Alert,
    FlatList,
    SafeAreaView,
    StyleSheet,
    useWindowDimensions,
    TouchableOpacity,
} from 'react-native';
export default function Dashboard() {
  return (
    <View>
      <Text>Dashboard</Text>
    </View>
  )
}